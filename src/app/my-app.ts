import { GenderList } from './gender-list';
export class MyApp {
    id: number;
    Name: string;
    DOB: number;
    Gender:number=0;
    AdharCard:string;
    constructor(values: Object = {}) {
        // Object.assign(this, values);
    }
}
